<p id="notice"><%= notice %></p>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Foto</th>
      <th>Beschreibung</th>
      <th>Preis</th>
      <th>Zustand</th>
      <th>Datum</th>
    </tr>
  </thead>

  <tbody>
    <% @offer_blackboards.each do |offer_blackboard| %>
      <tr data-link="<% offer_blackboard%>">
        <td>Foto</td>
        <td rowspan="2"><%= link_to offer_blackboard.title, offer_blackboard %>
        <br>
          <%= offer_blackboard.description %>
        </td>
        <td><%= offer_blackboard.totalPrice %></td>
        <td><%= offer_blackboard.condition %></td>
        <td><%= offer_blackboard.created_at %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Offer blackboard', new_offer_blackboard_path %>



  <% if user_signed_in? %>
      <li>
        <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
      </li>
  <% else %>
      <li>
        <%= link_to('Login', new_user_session_path)  %>
      </li>
  <% end %>