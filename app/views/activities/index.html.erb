<p id="notice"><%= notice %></p>

<h1>Listing Activities</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Telephone</th>
      <th>Email</th>
      <th>Contactname</th>
      <th>Type</th>
      <th>Weekday</th>
      <th>Street</th>
      <th>Zipcode</th>
      <th>City</th>
      <th>LocationName</th>
      <th>LocationDescription</th>
      <th>Longitude</th>
      <th>Latitude</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @activities.each do |activity| %>
      <tr>
        <td><%= activity.title %></td>
        <td><%= activity.description %></td>
        <td><%= activity.telephone %></td>
        <td><%= activity.email %></td>
        <td><%= activity.contactName %></td>
        <td><%= activity.type %></td>
        <td><%= activity.weekday %></td>
        <td><%= activity.street %></td>
        <td><%= activity.zipcode %></td>
        <td><%= activity.city %></td>
        <td><%= activity.locationName %></td>
        <td><%= activity.locationDescription %></td>
        <td><%= activity.longitude %></td>
        <td><%= activity.latitude %></td>
        <td><%= link_to 'Show', campu %></td>
        <td><%= link_to 'Edit', edit_campu_path(campu) %></td>
        <td><%= link_to 'Destroy', campu, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Activity', new_activity_path %>
